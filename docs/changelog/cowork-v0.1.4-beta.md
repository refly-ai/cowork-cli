# cowork-v0.1.4-beta

- Validate the zero-mount Docker task flow with authenticated `opencode`, `gh`, and SSH access against `powerformer/consensus`.
- Validate the new beta release workflow and changelog gate before the next stable cut.
- Keep `preview`, `contribute`, and `resource` aligned as guide-only commands with explicit manual-execution metadata.
