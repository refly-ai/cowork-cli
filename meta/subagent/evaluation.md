# Subagent 评估策略

用于定义本仓库自检质量评估方法，支持横向比较与回归追踪。

## 评估目标

- 衡量验证结果的可信度与稳定性。
- 对比不同方案的执行成本与失败率。
- 为后续自动化提供结构化基线。

## 自检闭环

1. 在 `meta/docker/Dockerfile` 环境执行验证。
2. 用 `meta/subagent/acceptance.md` 做通过判定。
3. 将问题样例沉淀到 `meta/subagent/questions/`。
4. 用本文件指标复盘并更新下一轮策略。

## 建议指标

1. 成功率：验证步骤首次通过比例。
2. 复现率：同一流程重复执行结果一致比例。
3. 故障定位时长：从失败到定位根因的耗时。
4. 修复回归率：修复后再次失败的比例。

## 打分模板（可选）

- 可靠性（0-5）：
- 可复现性（0-5）：
- 可维护性（0-5）：
- 自动化友好度（0-5）：
- 总分（0-20）：

## 记录模板

- 日期：
- 验证对象：
- 验证版本：
- 指标结果：
- 结论：
- 后续动作：

## 最近一次记录

- 日期：2026-03-05
- 验证对象：`meta/docker/Dockerfile`（helloworld cold-start）
- 验证版本：`opencode v1.2.16`
- 指标结果：成功率 100%（2/2 通过）；复现率 100%（两次结论一致）
- 结论：在零挂载约束下，auth 文件 COPY 路径可用，最小冷启动稳定通过
- 后续动作：将 `auth.local.json` 作为本地私有文件注入（已加入 `.gitignore`），继续扩展到 cowork 命令闭环验证

## 最近一次记录（产品冒烟）

- 日期：2026-03-05
- 验证对象：Docker 冷启动中的 install.sh 安装链路
- 验证版本：`cowork-v0.1.0`（linux x86_64 产物）
- 指标结果：命令可下载但不可运行（`GLIBC_2.39 not found`），完成率 0%
- 结论：当前 Linux 发布产物与 Debian bookworm 冷启动环境存在运行时兼容性断层
- 后续动作：将 Linux 产物基线兼容性纳入发布验收 gate（或补充 musl 产物）
